# Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=4000
NODE_ENV=development

# Frontend URL (for CORS)
# For production, use your deployed frontend URL
# For multiple origins, separate with commas: http://localhost:3000,https://your-app.vercel.app
FRONTEND_URL=http://localhost:3000

# Database Configuration (Optional - defaults to books.db)
# DATABASE_PATH=books.db

# Auth0 Configuration (Required)
# Get these values from your Auth0 Dashboard
# 1. Domain: Found in Applications > Settings > Domain
# 2. Audience: Found in APIs > Your API > Identifier
AUTH0_ISSUER_URL=https://your-tenant.auth0.com
AUTH0_AUDIENCE=https://book-dashboard-api

# Vercel Configuration (Auto-set by Vercel, no need to set manually)
# VERCEL=1

